import scala.annotation.StaticAnnotation

package cloud {
  package zeitgeist {
     package lambda {
       package api {
         package http {
           package macros {
             // TODO this should be replaced with proper value from plugin
             class LambdaHTTPApiInternal extends StaticAnnotation
           }
         }
       }
     }
  }
}